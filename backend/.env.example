DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agent_assist"
REDIS_URL="redis://localhost:6379"
OPENSEARCH_URL="http://localhost:9200"
S3_ENDPOINT="http://localhost:9000"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET="agent-assist-dev"
S3_REGION="us-east-1"
JWT_SECRET="replace-with-openssl-rand-hex-32"
NEXTAUTH_SECRET="replace-with-openssl-rand-hex-32"
NEXTAUTH_URL="http://localhost:3000"
PORT="4000"
HOST="0.0.0.0"
NODE_ENV="development"
LOG_LEVEL="debug"
AGENT_GATEWAY_TIMEOUT_MS="30000"
# Rate limit: requests per user/tenant per minute (raise if you see 429 in heavy usage)
AGENT_GATEWAY_RATE_LIMIT_PER_USER="200"
AGENT_GATEWAY_RATE_LIMIT_PER_TENANT="1000"
CORS_ORIGIN="http://localhost:3000"
# Example agents (list + dedicated threads): true in dev when unset; set to false to hide in non-production
# ENABLE_EXAMPLE_AGENTS="true"
